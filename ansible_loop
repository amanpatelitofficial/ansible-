---
- name: Create directories on remote hosts
  hosts: all
  tasks:
    - name: Create directories using a loop
      file:
        path: "{{ item }}"  # Pre defined var ( item )
        state: directory
      with_items:        
        - /tmp/dir1
        - /tmp/dir2
        - /tmp/dir3

